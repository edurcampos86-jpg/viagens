# Python
__pycache__/
*.py[cod]
*.so
.venv/
venv/

# OAuth secrets (never commit)
client_secret.json
token.json
*.credentials

# Environment / secrets (v2.0)
.env
.env.local
.env.*.local
secrets.*
*.secret

# Node / Vite
node_modules/
dist/
.vite/
package-lock.json

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Temp
*.log
.cache/

# Claude Code (preferências pessoais locais — settings compartilhadas vão em .claude/settings.json)
.claude/settings.local.json

# Ingestão (Fase 3) — fotos brutas do Google Takeout
# Só vai pro repo o otimizado em /media/<trip-id>/, com EXIF stripado.
media-import/
proposals.json
# INGEST-LOG.md fica tracked (historico do que cada apply rodou)
