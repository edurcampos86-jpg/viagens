{
  "name": "viagens",
  "version": "2.0.0-alpha.0",
  "private": true,
  "description": "Portal de viagens do Eduardo Campos — memória + planejamento (v2.0)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint \"assets/**/*.js\" \"src/**/*.js\"",
    "lint:fix": "eslint --fix \"assets/**/*.js\" \"src/**/*.js\"",
    "format": "prettier --write \"src/**/*.{js,css,html,md}\" \"docs/**/*.md\"",
    "format:check": "prettier --check \"src/**/*.{js,css,html,md}\" \"docs/**/*.md\""
  },
  "devDependencies": {
    "eslint": "^9.18.0",
    "eslint-config-prettier": "^9.1.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2",
    "vite": "^6.0.7"
  }
}
